// sur inginious il faut mettre Circuit2 à la place de Circuit 
CHIP Circuit {
    IN a, b, c;
    OUT out1, out2;
    
    PARTS:
    // Commencez ici
    Or(a=a, b=b, out=out1);
    Not(in=c, out=out2);
}